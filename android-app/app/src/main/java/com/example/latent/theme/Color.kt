package com.example.latent.theme

import androidx.compose.ui.graphics.Color

// ── Latent Cam True-Black Palette ──────────────────────────────────────
val CameraBlack       = Color(0xFF000000)   // True black background
val CameraBody        = Color(0xFF0D0D0D)   // Subtle camera body surface
val CameraDarkGray    = Color(0xFF1A1A1A)   // Controls area
val CameraGray        = Color(0xFF2A2A2A)   // Elevated surfaces (dials, cards)
val CameraMidGray     = Color(0xFF4A4A4A)   // Inactive elements
val CameraLightGray   = Color(0xFFB0B0B0)   // Secondary text

// Accent: Leica Red — warm, premium, intentional
val LeicaRed          = Color(0xFFE63946)
val LeicaRedDim       = Color(0xFF8B2230)

// Film name typography accent — warm Kodak gold
val KodakGold         = Color(0xFFD4A853)
val KodakGoldDim      = Color(0xFF8A6E35)

// White for primary text and active indicators
val PureWhite         = Color(0xFFFFFFFF)
val SoftWhite         = Color(0xFFE0E0E0)

// Shutter ring
val ShutterRing       = Color(0xFFCCCCCC)
val ShutterCenter     = Color(0xFF1A1A1A)

// Recording / active capture
val RecordingRed      = Color(0xFFFF3B30)
